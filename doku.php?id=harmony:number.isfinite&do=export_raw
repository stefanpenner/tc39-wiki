====== ''Number.isFinite'' ======

An ''isFinite'' function is added to the ''Number'' constructor. It could be implemented as this:

<code javascript>
(function (global) {
    var global_isFinite = global.isFinite;

    Object.defineProperty(Number, 'isFinite', {
        value: function isFinite(value) {
            return typeof value === 'number' && global_isFinite(value);
        },
        configurable: true,
        enumerable: false,
        writable: true
    });
})(this);
</code>

This is better than the global ''isFinite'' because it is not confused by type coercion.

 --- //[[crock@yahoo-inc.com|Douglas Crockford]] 2010/12/15 20:22//
 --- //[[brendan@mozilla.org|Brendan Eich]] 2011/04/28 00:19//
